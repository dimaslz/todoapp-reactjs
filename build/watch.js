// 'use strict';

// var gulp = require('gulp'),
//     plugins = require('gulp-load-plugins')(),
//     runSequence = require('run-sequence'),
//     server = require('gulp-develop-server'),
//     paths = require('./paths');


// // function run(tasks) {
// //     return function() {
// //         return runSequence(tasks);
// //     }
// // }


// gulp.task('watch', function() {
//     // var copyFiles = [].concat(paths.images, paths.views, paths.fonts);

//     // plugins.watch(paths.scripts.libs, run(['js-libs']));
//     // plugins.watch(paths.scripts.src);
//     // plugins.watch(paths.styles, run(['sass']));
//     // plugins.watch(copyFiles, run(['copy']));
//     gulp.watch('./server/**/*.js', server.restart);
// });
